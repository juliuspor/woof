{
  "name": "woof-workspace",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/woof"
  ],
  "overrides": {
    "@sveltejs/vite-plugin-svelte": "7.2.0",
    "cookie": "0.7.2",
    "vite": "8.2.0"
  },
  "scripts": {
    "check": "npm run check --workspace apps/woof",
    "dev": "npm run tauri:dev --workspace apps/woof",
    "build": "npm run tauri:build --workspace apps/woof",
    "test": "npm run test --workspace apps/woof"
  }
}
